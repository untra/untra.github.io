source 'https://rubygems.org'

# Mirrors the gem set used by GitHub Pages' legacy build,
# so the GitHub Actions build matches what was previously deployed.
# https://pages.github.com/versions/
gem 'github-pages', group: :jekyll_plugins
